export function updateGame(array: Array<Array<0 | 1>>): Array<Array<0 | 1>> {
  let result: Array<Array<0 | 1>>;
  for (let i = 0; i < array.length; i++) {
    for (let j = 0; j < array[i].length; j++) {
      
    }
  }
  return result;
}
